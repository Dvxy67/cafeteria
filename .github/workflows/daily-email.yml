name: Test Cafeteria

on:
  schedule:
    - cron: '41 13 * * 1-5'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test simple
        run: |
          echo "🚀 Workflow fonctionne !"
          echo "Heure UTC: $(date -u)"
          echo "Cron prévu: 13:10 UTC (15:10 France)"